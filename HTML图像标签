<img>标签用于定义HTML页面中的图像 单标签

<img src= “图像URL”/>

单词image的缩写，意为图像。

src是<img>的标签的必须属性，它用于制定图像文件的路径和文件名

所谓属性：简单理解就是属于这个图像标签的特性

注意：

1、图像标签可以拥有很多属性，必须写在标签的后面

2、属性之间不分先后顺序，标签名与属性、属性和属性直接均用空格分开

3、属性采取键值对的格式，即key= “value”的格式，属性= “属性值”

<img src=""/>
