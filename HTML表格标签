表格标签
1、作用：用于显示、展示数据
2、语法：
     <table>
       <tr>
         <td>单元格内的文字</td>
         ....
       </tr>
       </table>

3、表头单元格标签<th></th> 里面的文字加粗并居中显示

4、表格属性：
     align=”center\left\right”对齐
     border=”1...”边框
     cellpadding 设置单元格文字与边框距离
     cellspacing设置单元格与单元格之间的距离

5、表格结构标签
a.<thead></thead>:用于定义表格的头部，<thead>内部必须拥有<tr>标签。一般是位于第一行。
b.<tbody></tbody>:用于定义表格的主体，主要用于放数据本体
c.以上标签是放在<table></table>标签中 

6、合并单元格
跨行合并：rowspan=”合并单元格的个数”
跨列合并：colspan=”合并单元格的个数”

目标单元格：（写合并代码）
跨行：最上侧单元格作为目标单元格
跨列：左侧单元格

表格总结

1、表格标签
  table 标签、tr行标签、td单元格标签、th表头单元格标签

2、表格属性
